@{
    Layout = "../Shared/_Layout.cshtml";
}

@section header{
    Quản lý hệ số lương
}

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Danh sách hệ số lương
            </div>
            <div class="panel-body">
                <div class="row search-form form-group">
                    <div class="col-lg-6">
                        <div class="input-group">
                            <input type="text" id="txtSearch" value="@ViewBag.SearchString" class="form-control" name="keyword" placeholder="Nhập hệ số lương cần tìm...">
                            <span class="input-group-btn">
                                <button class="btn btn-default" type="submit" onclick="loadData()">Tìm kiếm</button>
                            </span>
                        </div>
                    </div>
                    <div class="col-lg-4"></div>
                    <div class="col-lg-2">
                        <button type="button" class="btn btn-success" data-toggle="modal" data-target="#myModal" onclick="clearTextBox();">Thêm hệ số lương</button>
                    </div>
                </div>
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Hệ số lương</th>
                                <th>Lương cơ bản</th>
                                <th>Hành động</th>
                            </tr>
                        </thead>
                        <tbody class="tbody"></tbody>
                    </table>
                    @*@Html.PagedListPager(Model, page => Url.Action("List", new { page, @keyword = @ViewBag.SearchString }))*@
                </div>
            </div>
        </div>
    </div>
</div>
@section assets
{
    <script src="/Assets/Admin/js/dateFormat.js"></script>
    <script src="/Assets/Admin/js/luong.js"></script>
}

@RenderPage("../Luong/Detail.cshtml")
